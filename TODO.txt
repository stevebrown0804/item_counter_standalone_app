---------------------------------------Public use--------------------------------------------------

TODO: When we click the "Skip this step next time" button on the 2nd confirmation window of "Delete outdated transactions," it appears to apply the setting....but it's not visibly moving the switch.  Let's move the switch and confirm that the underlying behavior is being adopted.

TODO (Switch added but not implemented yet): Add a switch on the settings screen for "Settings sheet interactions immediately return you to the home screen"
    *The following this will return the user directly to the home screen:
        * Clicking the back button on the Transaction Viewer
        * Clicking the save button in the Averaging Window area
        * Selecting a new Time Zone
        * Clicking the back button on the Edit Countable Items sheet
        * Completing the "Export database" process
        * Finishing the "Import database" process (successfully or not)
        * Finishing the "Delete outdated transactions" process (unless it's canceled/aborted)
        * Toggling the "Skip second confirmation..." switch
    * btw we'll have any toasts show up after returning to the home screen

TODO: Give ChatGPT a DB exported from the version running out our phone and a copy of the db.*.dart files, and tell it to generate a migration.
    * Run the (debug version, on the emulator) app with that DB file in-place and see if it brings it up-to-date with the current schema.
    * Assuming it does, then I ~think~ that means we can deploy versions-in-development to our phone, with confidence that the app will bring the DB up to speed.
    FOLLOW-UP: I did a preliminary version of this where I just pasted in an existing DB file and the db.*.dart files and asked if there were differences, of which there were 2:
        * some missing rows (date pinning-related) in the settings table
        * a uniqueness contraints on the items.display_order field

TODO: the DB import process is likely saying that existing DB files are incompatible because they lack some rows from the settings table; let's try to add those rows before we have the app decide it the DB file is compatible or not.

TODO: Add a 'reset' (or 'clear', maybe) button to the RHS of the end Date textbox (in the Averaging Window area); have it revert the contents of the textbox to 'Today' (and force the 'pin end date' switch to off)

TODO: Answer the question, "With the start date and end date, we're assuming mm/dd/yyyy, as far as I can tell.  Does Android change that, in other regions (where yyyy/mm/dd, etc. are common)?  Do we need to adapt (ie. by adding some i18n to the project)? TBD"

TODO: Answer the question, "Do we want a 'Use default path' switch beneath the Export Database button?  If it's disabled we can let the user choose the path to which to save the DB file."
    * (Or we might reword the switch and have its default state be off; we'll see.)
    * btw This introduces the possibility of the DB export process being canceled; we should consider that when thinking about the "Settings sheet interactions...." switch's behavior (when set to 'on').

TODO: Add a "use timezones or do not....there is no try" (wording to be adjusted later) switch to the Settings sheet; default value: On
    * On -- use the existing timezone functionality
    * Off -- hide the timezone-related stuff, I guess; just use UTC, but keep the specifics "as hidden as we can manage, without it becoming ridiculous"

TODO: In the Transaction Viewer, let's either have the "Apply filter" button be lit up when any UI interaction takes place while would necessitate it being clicked before the new filter is changed, or just get rid of it.
    Or both, and allow a setting to control which.  *shrug*

TODO: The Transaction Viewer's state/end dates extend well beyond the range of existing transactions within the DB; let's trim down the allowable dates to ones that actually have transactions.

TODO (Lower priority): Item metadata
    * Create a table to hold key/value pairs
    * Create a UI for defining those pairs
        * atm I'm thinking "one set of keys that applies to the entire item set" (...although that's making me think about ways to allow multiple item sets.🤔  we'll think about that later, though.)
         * eg. Manufacturer, Product Name
    * Add this table as an option for importing

TODO (POSSIBLY OUT-OF-DATE; NEEDS CLARIFICATION): Time-zone specifying
    * Add a "Specify time zone by alias" checkbox  <---4/21/26 Where, exactly?🤨 Settings sheet?
        * If it's checked, present a button labeled "Specify a time zone alias"  <---4/21/26 Again...where?🤨 In a modal dialog?
            * Pressing that button brings up a sheet of time zone aliases and an initially-empty column (except for UTC, I suppose) of tz aliases.  Let the user either tap an alias and pressed a button labeled "Enter alias" to bring up a window with an input box in which they can enter an alias (and tap an "OK" button or a "Cancel" button), or long-tap the alias field beside an alias and edit the alias in-place.
            * When the user taps in the "Time Zone" input box, it brings up a list of aliases in the DB.  (That is, it does the current behavior for selecting an alias.)
        * If the box is unchecked:
            * When the user taps into the "Time Zone" input box, it presents a complete list of tz names
            * As the user enters characters, it refines that list to names matching the substring that the user as entered.  It continues like this until the user either taps "Submit" or taps the name (which then populates the input box), then taps "Submit."
    *Add a setting with key derived from "Specify time zone by alias"; default value is false (ie. unchecked)

        sub-TODO: Answer the question, "Seeing as how the previous TODO doesn't really make a lot of sense, should we edit it until it makes sense or just scrap it and start over with an "Add a TZ alias-adding UI" TODO, which sounds much more straightforward?"
            RESPONSE: Yes, at this point I absolutely think we SHOULD! ...because heck if I can make sense of that TODO.😂

TODO: Add a drop-down or something in the settings sheet to select the summary statistic
    Possible default: With the 'monthly average' statistic selected and the drop-down disabled.
    A THOUGHT: This makes me think about "items sets" and having multiple counting UIs in the app; in THAT context, implementing "various" summary statistics seems more useful.  As it is, though, this kinda just feels like a placeholder for "when someone else wants to add a new summary statistic."

TODO (After 'pinning dates' and 'summary statistic selection drop-down'):
    sub-TODO: Merge these two TODOS
        * Adjust the column header on the home screen to reflect whether there's a fixed interval (which is how it currently behaves), a pinned start date (eg. "[beginning date] to today") or a pinned date range (eg. "[beginning date] to [end date]")
        * Add a statement about "Current averaging window" (although we probably shouldn't say anything that's specific to the current summary statistic, if we're planning to let it be changed) (-- unless we make that part of the summary statistic's settings; ie. "thing to call the summary statistic in the 'Current [summary statistic]' line")
              Have it say stuff like:
                  (for 'a duration'): "[# days ago] days ago through today"
                  (for 'a pinned start date'): "[pinned start date] through today"
                  (for a 'pinned duration'): "[pinned start date] through [pinned end date]"
              * We should probably also state this on the home screen...at least, if it's at all unusual (ie. pinned durations).  Maybe change the header of the averages column?  We'll think about this.

TODO (after adding a TZ alias-adding UI): "It's like a new-DB wizard, kinda"
    When a DB's items table is empty, replace the 'add' button on the home screen with an 'add items' button, which brings up the 'edit countable items' sheet.
    A THOUGHT: Do we want this same wizard to also help set up time zone aliases, or at least ask if the user wants to use them?  If so, we should probably wait until we have a TZ-adding UI before we do this.

TODO re: Database importing:
    * For the time being, the app's going to enforce "only import (item transactions / settings) if (items / time zones) (respectively) is imported;" we should communicate this somewhere, so as not to overly-aggravate those trying to import one table, then import the 2nd table in a later operation.
    * Whatever directory the "import DB" file picker starts in, we should also communicate that directory that is (to reduce guesswork about where best to place a DB file).

TODO: Answer the question, "When it comes to the end date, do we want to allow (and parse) arithmetic formulas involving the word 'today,' such as "today - 1" (for 'yesterday')?  It seems "possibly" useful to someone...maybe.
    * A THOUGHT: Maybe this is the sort of thing we should let someone who WANTS that feature, to implement it.

TODO (MAYBE): Rather than allowing end dates in the future, we might add some sort of "give a fixed date range a target date in the future, but have the actual interval be (start date) to (today), then move the interval forward by a day, each day until that target end date."  Maybe.  Either way, I want to avoid allowing the user to pick interval start/end dates in the future.

---------------------------------------
Polishing

*Settings sheet
TODO: Switches are pretty nice-looking, and do basically the same thing as checkboxes, although they kinda imply on/off-ness, which seems more specific than checkbox behavior.
    Still, let's look for checkboxes (or possibly other UI elements) that we can replace with switches.
        FOLLOW-UP: I'm only seeing one atm: The "Skip 2nd confirmation" checkbox/save button.  We'll keep our eyes open for others, though.

*Averaging Window area
TODO: (Background) With the "Averaging window" textboxes, we're using TextPainter to determine the minimum interior width necessary to display things (assuming the longest possibly averaging window is 99999 days); thing is, it's producing widths wider than necessary.
    (Actual TODO): Answer the question, "Is the font being used by TextPainter to make those measurements the same font that's actually used to render the elements and their contents?  "Not using the right font" seems like the kind of bonehead mistake ChatGPT might make."
TODO: Restrint the allowable input in the End Date textBox to "2 digits for mm dd, 4 digits for yyyy, and slashes/hyphens/[spacebar]/other separation characters."  (This is in addition to "the date strings that the date picker returns.)"
TODO: Eith the End Date Textbox, if the current date is selected via the date picker and the "Pin end date" box is NOT checked", display "Today"

*Edit countable items sheet
TODO: Intelligently collapse the display order values
    For example, if we change a display order n to n+1 or higher, reduce the in-between row's display orders by one.  If we change a display order n to n-1 or lower, increase the in-between row's display orders by one.
TODO: Add a confirmation to the delete button, eg. a dialog saying "This will delete # transactions, along with the item; proceed?" along with "Abort" and "Proceed" buttons.  If # is 0, we can probably skip this confirmation.
    Also, add a checkbox to the settings sheet for "Skip confirmation when deleting items" or some-such.
TODO: Make the rows & the "add row" button the scrollable region, so that the 'submit' button stays in place on-screen
TODO: Make the textBox and the drop-down box the same vertical height
TODO: Get rid of that light gray bar separating each row, or perhaps add some space between it and the controls it encloses, then add an exterior border so that we have a box around the scrollable region
TODO: Answer the question, "Does closing the 'edit countable items' screen via the submit button look weird to you?  It looks slightly chaotic to me, which could probably be smoothed out -- unless it's being caused by something specific to the emulator or something."

*code
TODO: Try using TextPainter to get the maximum width of things like 2 (or possibly 3- or 4-) digit numbers and using that to determine the width of the input boxes.
TODO: Man....there are a lot of hard-coded values in this code.  And every one of them makes me think, "How much will this suck for someone with an unusual desktop size/scaling factor/font/etc.?  c'mon now."
    Anyhow, let's work on getting rid of hard-coded constants and replacing them with "formulas based on information from the OS."


---------------------------------------------------------------------------------------------------

[REMINDER: From here down we have TODOs written during the first pass at app development; they may or may not still apply.  We'll get around to evaluating them 'eventually.']

---Minor initiatives---


---Major initiatives---

TODO (Once we've implemented Time-zone specifying and "Add item(s) to count"): Create a "new install" wizard that prompts the user to do those two things (or possibly import a DB or something)

TODO: UI customizing
    * Let the user long-tap the (modifiable) text fields to begin editing them
        * Upon editing, store those values in the settings table in the DB
    * Also give a screen under settings for seeing/editing these values
        * Button label: "Customize UI" <---or something along these lines.  tbd.
        * Have that button bring up a sheet with (LHS) description (eg. AppBar Title) and (RHS) current value
            * Let the values on the RHS column be modified there; update the app immediately with any changes
        * Sounds like this sheet just needs a "Close" button
        * Although...that depends on the UI for editing a text field.  In-place?  Modal dialog w/ input box? tbd.

TODO: UI minutiae
    Add a button to the settings sheet with label "UI minutiae"; it'll bring up a sheet of settings of various minutiae.
    eg.:
        * checkbox: auto-return to main sheet after the user clicks 'Save' on the settings sheet
            * Default value: false, I guess? *shrug*
        * checkbox: 'Show time zone'
              * Default value: True
              * When it's checked, show the "Time zone: [Alias/name]" box beneath the app title on the main view
              * When it's unchecked, hide that text
        * sliders controlling the amount of vertical paddings between rows, maybe?  *shrug*
        * (tbc as we think of things to add)



---UI stuff---

TODO (MAYBE): Make the Transaction Viewer sheet more compact
    Maybe move the "last # days" radio onto the first row, compress the input boxes vertically, remove space between the "All" row and the "Apply filter" button, etc.



---TBD---
TODO: Give a way to choose other summary statistics (which we probably won't implement, but maybe someone else will want to)

    ChatGPT describes the current summary statistic as:
    "In plain terms, the statistic is:
        Average per day over the last N days (including days with no logs).

    More precise description you can use in a setting name/tooltip:
        “For each pill, sum all quantities from max(today − (N−1) days, first transaction date) through today (inclusive), then divide by the number of calendar days in that range, counting days with zero intake.”"



---MAYBE---

TODO (MAYBE): Let the user long-tap & drag them to rearrange the rows of the settings sheet




--------------------------------------Personal use-------------------------------------------------

TODO (Once we've implemented the backend project as a WASM module): Create a branch where we remove the FFI stuff and slot in a WASM module.  See which one's shorter (or simpler).
